{
  "schema_version": "2.0.0",
  "namespace": "hpc",
  "capabilities": {
    "overview": {
      "summary": "The canary_hpc extension batches Canary jobs and submits batches to HPC schedulers through hpc_connect."
    },
    "batching": {
      "primary_objects": [
        "BatchingSpec",
        "BatchSpec",
        "TestBatch",
        "CanaryHPCConductor",
        "HPCConnectBatchRunner"
      ],
      "layouts": {
        "flat": "Jobs within a batch do not depend on each other. Batches may depend on other batches.",
        "atomic": "Dependency-connected components are kept together. Batches do not depend on other batches."
      },
      "node_policies": {
        "same": "All jobs in a partition require the same node count.",
        "any": "Jobs with different node counts may be batched together."
      },
      "targets": {
        "duration": "Create batches targeting approximate simulated runtime.",
        "count": "Create at most N batches.",
        "count=max": "Create the maximum number of batches allowed by the selected layout."
      },
      "workspace_layout": {
        "batch_lock": ".canary/sessions/<session>/batches/<id[:7]>/batch.lock",
        "batch_script": ".canary/sessions/<session>/batches/<id[:7]>/canary-inp.sh",
        "batch_stdout": ".canary/sessions/<session>/batches/<id[:7]>/canary-out.txt",
        "note": "Batch directories are session-scoped artifacts under .canary/sessions/, not under .canary/cache/."
      }
    },
    "execution": {
      "summary": "HPC batches are submitted through hpc_connect. Each batch receives a generated resource pool and then launches Canary inside the allocation.",
      "environment": {
        "CANARY_LEVEL": "Incremented for nested Canary execution.",
        "CANARY_HPC_BATCH": "Current batch identifier.",
        "CANARY_HPC_NODE_COUNT": "Number of scheduler nodes requested."
      },
      "timeouts": {
        "queue_timeout": "Maximum time waiting for scheduler start.",
        "run_timeout": "Estimated batch runtime multiplied by timeout multiplier."
      }
    },
    "commands": {
      "run_with_backend": "canary hpc run --backend BACKEND PATH",
      "legacy_run_options": "canary run --hpc-backend BACKEND PATH",
      "batch_spec": "Use --batch-spec or legacy -b spec=...",
      "batch_log": "canary hpc log <BATCH_ID>  -- print the stdout log for a batch",
      "query_batch": "canary query batch <BATCH_ID>  -- inspect a batch.lock as JSON",
      "query_batch_path": "canary query batch <BATCH_ID> .status  -- JSON path into batch.lock",
      "query_batches": "canary query batches  -- list all batches for latest session as JSON array",
      "query_batches_where_string": "canary query batches --where status.category==PASS",
      "query_batches_where_numeric": "canary query batches --where 'timings.queue_wait>3600'  -- numeric timing filter",
      "query_batches_where_running": "canary query batches --where 'timings.running<60'",
      "query_batches_session": "canary query batches --session 2026-09-03T18-10-19"
    },
    "query_batches_output_fields": {
      "summary": "Each row returned by canary query batches includes:",
      "fields": {
        "id": "Full UUID of the batch.",
        "id_prefix": "7-char batch directory name.",
        "session": "Session name this batch belongs to.",
        "job_count": "Number of jobs in this batch.",
        "jobs": "Array of {id, name} for each job in the batch.",
        "estimated_runtime": "Packer's estimated makespan in seconds.",
        "algorithm": "Packing algorithm used.",
        "node_count": "Number of scheduler nodes requested.",
        "width": "CPUs per node.",
        "status.category": "PASS or FAIL (or null if still running).",
        "status.outcome": "DONE, FAILED, TIMEOUT, etc.",
        "status.reason": "Human-readable reason string.",
        "timings.total": "Total wall time from submission to completion (seconds).",
        "timings.queue_wait": "Time waiting in the scheduler queue (seconds). Use this with --where for scheduling triage.",
        "timings.running": "Time actually running inside the allocation (seconds).",
        "submitted_on": "ISO8601 timestamp of batch submission."
      },
      "where_numeric_examples": [
        "canary query batches --where 'timings.queue_wait>3600'    # queue-starved batches",
        "canary query batches --where 'timings.running<60'         # fast-completing batches",
        "canary query batches --where 'job_count>100'              # large batches"
      ],
      "none_note": "timings fields are null for batches that have not yet completed. Ordered --where predicates return False for null values."
    },
    "resource_model": {
      "summary": "The conductor uses backend node resource counts to generate a topology-aware resource pool for each batch.",
      "multi_node": "Batches may request multiple scheduler nodes when jobs require multiple Canary nodes."
    },
    "debug_logging": {
      "summary": "Run with --debug or -v -v to enable DEBUG-level logging captured in .canary/logs/canary.0.log.",
      "logged_at_debug": [
        "Batch workspace root path",
        "Per-batch workspace directory, job count, and dependency count at creation",
        "Batch packing summary: algorithm, total batches, total jobs",
        "Per-batch packing row: id, jobs, nodes, estimated runtime",
        "Batch submission: id, estimated runtime, dependency count",
        "Batch completion: id, rc, pass/fail counts, total wall time, queue wait, running time",
        "Stale cache cap firings: which jobs had their cached mean clamped to declared timeout"
      ]
    }
  }
}
