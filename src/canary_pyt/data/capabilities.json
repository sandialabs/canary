{
  "schema_version": "2.0.0",
  "namespace": "pyt",
  "capabilities": {
    "overview": {
      "summary": "The canary_pyt extension provides the reference Python-based job definition format for Canary, commonly written as .pyt files.",
      "file_patterns": [
        "*.pyt"
      ],
      "generator": "PYTSpecGenerator",
      "model": "PYTModel",
      "loader": "PYTLoader",
      "adapter": "PYTAdapter",
      "emitter": "PYTLockEmitter"
    },
    "authoring": {
      "minimal_pattern": [
        "Place canary_pyt or canary directives at module scope.",
        "Place executable test logic in functions.",
        "Guard runtime execution with if __name__ == '__main__'.",
        "Exit with 0 for success or raise/use Canary failure helpers for structured outcomes."
      ],
      "minimal_example": [
        "import sys",
        "import canary",
        "",
        "canary.directives.keywords('smoke')",
        "",
        "def test() -> int:",
        "    return 0",
        "",
        "if __name__ == '__main__':",
        "    sys.exit(test())"
      ]
    },
    "lifecycle": {
      "summary": "A .pyt file may declare a per-file setup and/or teardown function via bare decorators. They run in the worker process that executes the job, wrapping the test body.",
      "decorators": {
        "canary_pyt.directives.setup": "Registers a per-file setup function. Runs before the test body via the canary_runteststart hook. If it raises, the job is marked broken and the body does not run. One per file.",
        "canary_pyt.directives.teardown": "Registers a per-file teardown function. Runs after the test body via the canary_runtest_finish hook, regardless of the body's outcome. One per file."
      },
      "behavior": [
        "During scanning the decorator records the function name onto the job spec attributes (__setup_fn__ / __teardown_fn__); at run time it is an identity decorator so the function stays defined.",
        "At run time canary_pyt imports the job's .pyt in-process and calls the recorded function with the live canary.Job as its sole argument, inside the job workspace.",
        "Setup/teardown are not separate jobs; they wrap the single test's execution."
      ],
      "example": [
        "import canary_pyt",
        "",
        "canary_pyt.directives.name('t')",
        "",
        "@canary_pyt.directives.setup",
        "def my_setup(job):",
        "    ...",
        "",
        "@canary_pyt.directives.teardown",
        "def my_teardown(job):",
        "    ..."
      ]
    },
    "directives": {
      "summary": "Directives are generation-time declarations captured by the .pyt loader and converted into JobSpecIR objects.",
      "common": {
        "testname": "Set one or more generated test families.",
        "name": "Alias for testname.",
        "keywords": "Attach keywords used for selection.",
        "owners": "Attach owner metadata.",
        "timeout": "Set job timeout.",
        "parameterize": "Generate job variants from parameter values.",
        "cpus": "Set fixed CPU requirement without creating a named parameter variant.",
        "gpus": "Set fixed GPU requirement without creating a named parameter variant.",
        "nodes": "Set fixed node requirement without creating a named parameter variant.",
        "depends_on": "Declare dependencies by name, pattern, or DependencySelector-like dictionary.",
        "copy": "Copy source files or directories into the job workspace.",
        "link": "Symlink source files or directories into the job workspace.",
        "sources": "Declare source files without staging them.",
        "artifact": "Declare result artifacts.",
        "baseline": "Declare rebaseline actions.",
        "exclusive": "Prevent concurrent execution with other jobs.",
        "load_module": "Load an environment module before execution.",
        "source": "Source an rc file before execution.",
        "set_id": "Set a stable explicit ID or ID template.",
        "set_attribute": "Attach arbitrary metadata.",
        "setup": "Decorator registering a per-file setup function; runs before the test body (via canary_runteststart) with the live Job. One per file.",
        "teardown": "Decorator registering a per-file teardown function; runs after the test body (via canary_runtest_finish) with the live Job, regardless of outcome. One per file.",
        "enable": "Enable or disable generated jobs conditionally.",
        "skipif": "Skip jobs conditionally with a reason.",
        "xfail": "Mark expected nonzero exit.",
        "xdiff": "Mark expected diff outcome.",
        "generate_composite_base_case": "Create an analysis/composite job depending on parameterized jobs.",
        "analyze": "Alias for generate_composite_base_case."
      }
    },
    "parameters": {
      "summary": "parameterize() creates named job variants. cpus(), gpus(), and nodes() set fixed resource requirements without changing the generated display name.",
      "resource_parameter_names": [
        "cpus",
        "gpus",
        "nodes"
      ],
      "parameter_space_types": [
        "list_parameter_space",
        "centered_parameter_space",
        "random_parameter_space"
      ],
      "common_mistakes": [
        "Using cpus() when named variants such as cpus=8 are desired; use parameterize('cpus', ...) instead.",
        "Duplicating a key between parameters and meta_parameters.",
        "Expecting top-level runtime code to be ignored during scan unless it is guarded."
      ]
    },
    "dependencies": {
      "summary": "depends_on() supports strings, dictionaries, lists, and DependencySelector objects.",
      "dictionary_keys": {
        "job": "Dependency name, ID, or pattern.",
        "when": "Dependency condition such as on_success, on_failure, always, or an outcome expression.",
        "expects": "Expected match count: '+', '?', '*', or a positive integer."
      }
    }
  }
}
