{
  "schema_version": "2.0.0",
  "namespace": "core",
  "capabilities": {
    "overview": {
      "what_is_canary": "Canary is a Python-based workflow and test execution framework for defining, scheduling, running, and inspecting computational jobs across environments ranging from developer laptops to HPC systems.",
      "core_model": "Canary core executes Job objects. It does not require one universal job-definition file format. Job generators interpret user-facing definitions and emit JobSpecIR or JobSpec objects. Canary resolves dependencies, constructs Jobs for a Session, schedules them with resources, runs them, and persists results.",
      "when_to_use": [
        "Run computational tests or simulations with structured status, logs, artifacts, and measurements.",
        "Define parameter sweeps and dependency-aware workflows.",
        "Execute jobs locally or through extension-provided execution backends.",
        "Expose structured results to scripts, CI systems, reports, or AI agents."
      ],
      "major_concepts": {
        "job_generator": "Interprets an input representation and emits JobSpecIR or JobSpec objects.",
        "jobspec": "A resolved description of a job: identity, command, parameters, resources, dependencies, assets, artifacts, and metadata.",
        "job": "A concrete session execution object constructed from a JobSpec and consumed by Canary's scheduler/executor.",
        "session": "A persistent execution run containing jobs, session metadata, measurements, and a session.lock manifest.",
        "workspace": "A persistent .canary directory containing specs, sessions, logs, cache, reports, a SQLite index, and temporary state.",
        "resource_pool": "Topology-aware model of available resources, organized by nodes and resource instances with slots.",
        "query": "A lightweight JSON path query command for job locks, session locks, capabilities, and skills."
      },
      "start_here": {
        "write_a_test": "tests.authoring",
        "run_work": "execution.local",
        "inspect_results": "query",
        "add_post_processing": "hooks.post",
        "write_a_generator": "plugins.generators",
        "agentic_workflows": "agent_workflows"
      }
    },
    "concepts": {
      "pipeline": {
        "summary": "Canary separates user-facing job definitions from execution. Generators create specs; the workspace resolves and stores specs; sessions construct jobs; the executor runs jobs and persists results.",
        "stages": [
          {
            "name": "definition",
            "description": "A user writes or supplies a job definition in a format understood by an installed generator."
          },
          {
            "name": "generation",
            "description": "A Job Generator emits JobSpecIR or JobSpec objects."
          },
          {
            "name": "dependency_resolution",
            "description": "Dependency selectors are resolved into concrete SpecDependency edges."
          },
          {
            "name": "selection",
            "description": "Rules filter or mask specs by keyword, parameters, owner, regex, path, IDs, resources, or rerun state."
          },
          {
            "name": "session_construction",
            "description": "A Workspace constructs Job objects from JobSpec objects for a Session."
          },
          {
            "name": "execution",
            "description": "A resource-aware queue schedules runnable jobs. Launchers execute job commands and update status, timekeeper, and measurements."
          },
          {
            "name": "persistence",
            "description": "Job lock files, session lock files, artifacts, logs, and a SQLite index persist execution state."
          }
        ],
        "important_distinction": "Canary core does not parse every supported job-definition format. Generators own input formats; Canary core owns execution of Jobs."
      },
      "objects": {
        "JobSpecIR": {
          "purpose": "Intermediate generator output that may contain unresolved dependency selectors.",
          "created_by": "Job Generators.",
          "resolved_to": "JobSpec after dependency resolution."
        },
        "JobSpec": {
          "purpose": "Resolved job specification with concrete dependency edges.",
          "contains": [
            "id",
            "file_root",
            "file_path",
            "family",
            "parameters",
            "meta_parameters",
            "dependencies",
            "keywords",
            "assets",
            "artifacts",
            "environment",
            "command",
            "timeout",
            "resource-related parameters"
          ]
        },
        "Job": {
          "purpose": "Concrete execution object in a Session.",
          "contains_runtime_state": [
            "status",
            "state phase",
            "timekeeper",
            "measurements",
            "workspace",
            "resource allocation",
            "dependencies as Job objects"
          ]
        }
      },
      "lifecycle": {
        "job_phases": [
          "PENDING",
          "STAGING",
          "RUNNING",
          "FINISHING",
          "DONE"
        ],
        "status_model": {
          "category": "Broad category such as PASS, FAIL, SKIP, CANCEL, or NONE.",
          "outcome": "Specific outcome such as SUCCESS, FAILED, DIFFED, TIMEOUT, SKIPPED, BLOCKED, ERROR, BROKEN, XFAIL, XDIFF, CANCELLED, or INTERRUPTED.",
          "reason": "Human-readable reason, usually populated for non-success outcomes.",
          "code": "Exit code or framework-assigned code."
        },
        "timekeeper": {
          "timestamps": [
            "_submitted",
            "_staged",
            "_started",
            "_stopped",
            "_finished"
          ],
          "durations": [
            "pending",
            "staging",
            "running",
            "finishing",
            "total"
          ]
        }
      }
    },
    "workspace": {
      "summary": "A Canary workspace is a persistent .canary directory containing generated specs, sessions, logs, cache, reports, temporary files, and a SQLite database.",
      "layout": {
        ".canary/workspace.sqlite3": "SQLite index/result/state store.",
        ".canary/sessions/<session>/session.lock": "Session manifest and session-level state.",
        ".canary/sessions/<session>/<job-path>/testcase.lock": "Serialized job state.",
        ".canary/logs": "JSON log files.",
        ".canary/reports": "Generated reports.",
        ".canary/cache": "Runtime caches and view metadata.",
        ".canary/tmp": "Temporary files including result spool data."
      },
      "commands": {
        "init": "canary init",
        "remove": "canary rm PATH",
        "info": "canary info"
      }
    },
    "jobs": {
      "summary": "A Job is a concrete execution object in a session. It wraps a JobSpec, an ExecutionSpace, dependency Job objects, status, lifecycle state, timekeeper, measurements, variables, and resource allocation.",
      "important_fields": {
        "id": "Stable spec/job identifier.",
        "spec": "Resolved JobSpec.",
        "workspace": "ExecutionSpace with root, relative path, and session.",
        "dependencies": "List of Dependency objects pointing to upstream Jobs.",
        "status": "Status category/outcome/reason/code.",
        "state": "JobState phase.",
        "timekeeper": "Lifecycle timestamps and duration helpers.",
        "measurements": "Structured key/value data recorded during execution.",
        "variables": "Runtime environment variables passed to the job."
      },
      "measurement_api": {
        "method": "Job.add_measurement(name, value)",
        "storage": "Serialized into testcase.lock and result records.",
        "query_example": "canary query -j JOBID measurements.data.<name>"
      },
      "readiness": {
        "is_ready": "True when all dependencies are done and satisfied.",
        "is_runnable": "False when done, skipped, or permanently blocked by failed dependency criteria.",
        "blocked": "A job can be marked BLOCKED when a dependency finishes with an outcome that does not satisfy its dependency condition."
      }
    },
    "sessions": {
      "summary": "A Session is a persistent execution run containing jobs, session name, prefix directory, measurements, job IDs, start/finish timestamps, and return code.",
      "lock_file": "session.lock",
      "measurement_api": {
        "method": "Session.add_measurement(name, value)",
        "query_example": "canary query -s latest measurements.<name>"
      },
      "lifecycle": {
        "start_hook": "canary_sessionstart(session)",
        "finish_hook": "canary_sessionfinish(session)",
        "run": "Session.run constructs a Runner for runnable jobs and invokes canary_runtests."
      },
      "latest": "The workspace refs/latest file points to the latest session directory."
    },
    "resources": {
      "summary": "Canary uses a topology-aware ResourcePool to prevent oversubscription and to assign runtime resource IDs to jobs.",
      "schema": {
        "root_keys": [
          "allow_multinode",
          "additional_properties",
          "nodes"
        ],
        "node_keys": [
          "id",
          "additional_properties",
          "resources"
        ],
        "resource_instance_keys": [
          "id",
          "slots",
          "properties"
        ]
      },
      "job_requests": {
        "source": "Job.required_resources returns a list of NodeRequest objects derived from JobSpec resource parameters.",
        "nodes": "A nodes parameter greater than 1 creates multiple per-node requests and treats multi-node jobs as whole-node reservations.",
        "cpus_default": 1,
        "gpus_default": 0
      },
      "runtime_environment": {
        "pattern": "CANARY_<RESOURCE_SINGULAR>_IDS",
        "examples": [
          "CANARY_CPU_IDS",
          "CANARY_GPU_IDS"
        ],
        "template_expansion": "Existing job variables may use %(cpu_ids)s or %(gpu_ids)s placeholders that are expanded after resource assignment."
      },
      "commands": {
        "show_pool": "canary config show resource_pool",
        "override_count": "canary run -r cpus=8 -r gpus=1 PATH",
        "resource_pool_file": "canary run --resource-pool-file FILE.yaml PATH",
        "oversubscribe": "canary run --oversubscribe cpus=2 PATH"
      }
    },
    "dependencies": {
      "summary": "Dependencies are declared by generators as selectors and resolved into concrete SpecDependency edges between JobSpecs.",
      "selector": {
        "type": "DependencySelector",
        "fields": {
          "pattern": "Name, full name, file path, ID, or glob pattern to match.",
          "expects": "Expected match count: '+', '?', '*', or a positive integer.",
          "when": "Condition required for dependent job to run."
        }
      },
      "conditions": {
        "on_success": "Dependency status category must be PASS.",
        "on_failure": "Dependency status category must be FAIL.",
        "always": "Dependency only needs to be done.",
        "*": "Alias for always in some generator inputs.",
        "expression": "Other strings may be evaluated as simple status/category expressions."
      },
      "common_mistakes": [
        "Using an ambiguous dependency pattern.",
        "Forgetting that dependencies match generated job names after parameter expansion.",
        "Expecting a downstream job to run when an upstream dependency failed but the condition is on_success."
      ]
    },
    "execution": {
      "local": {
        "summary": "Local execution uses a ResourceQueue and ResourceQueueExecutor to run jobs in worker processes while respecting dependencies and resource capacity.",
        "primary_command": "canary run PATH_OR_TAG",
        "single_job_command": "canary exec JOBID",
        "workers": "The --workers option controls the maximum process pool size, but resource availability may further limit concurrency.",
        "launcher": "The default launcher runs the job command as a subprocess in the job workspace."
      },
      "timeouts": {
        "job_timeout": "Job.total_timeout applies configured multipliers to the JobSpec timeout.",
        "session_timeout": "A session timeout may terminate all in-flight jobs.",
        "launcher_timeout": "The default subprocess launcher kills the process group on timeout."
      }
    },
    "query": {
      "summary": "canary query inspects JSON data from job locks, session locks, aggregated capabilities, or aggregated skills.",
      "selectors": {
        "-j, --job": "Query testcase.lock for a job found by ID or matching spec.",
        "-s, --session": "Query session.lock for a session name or prefix; latest is supported.",
        "-c, --capability": "Query the aggregated capability tree.",
        "-k, --skill": "Query the aggregated skill tree."
      },
      "syntax": {
        ".": "Return the whole selected object.",
        "foo.bar": "Access nested object keys.",
        ".foo.bar": "Leading dot is optional.",
        "array[0]": "Access list index.",
        "object['key with spaces']": "Access quoted key.",
        "object[\"key.with.dots\"]": "Access quoted key containing punctuation."
      },
      "examples": {
        "capability_root": "canary query -c .",
        "overview": "canary query -c overview",
        "post_hooks": "canary query -c hooks.post",
        "extension_list": "canary query -c ext --list",
        "extension_overview": "canary query -c ext.pyt.overview",
        "whole_job": "canary query -j JOBID",
        "job_measurements": "canary query -j JOBID measurements.data",
        "latest_session": "canary query -s latest",
        "skill_list": "canary query -k . --list",
        "skill_export": "canary query -k canary-orientation --markdown canary-orientation.md"
      },
      "not_jq": "The query language is intentionally small. It does not support filters, pipes, recursive descent, expressions, or transformations."
    },
    "hooks": {
      "summary": "Canary extensions use pluggy hooks to add commands, generators, resource behavior, launchers, runtime behavior, reports, measurements, lifecycle actions, capabilities, and skills.",
      "registration": {
        "entry_point_group": "canary",
        "plugin_manager": "_canary.pluginmanager.CanaryPluginManager",
        "marker": "canary.hookimpl or _canary.hookspec.hookimpl"
      },
      "query_data": {
        "canary_capabilities": "Return an extension capability document inserted under capabilities.ext.<extension>.",
        "canary_skills": "Return an extension skill document inserted under skills.ext.<extension>."
      },
      "pre": {
        "canary_sessionstart": {
          "purpose": "Runs when a new session starts.",
          "arguments": [
            "session"
          ],
          "agent_use": "Attach session-level structured metadata with session.add_measurement."
        },
        "canary_runteststart": {
          "purpose": "Runs during job setup before the job command executes.",
          "arguments": [
            "case"
          ],
          "default_behavior": "The built-in wrapper calls case.setup() and then saves the job."
        }
      },
      "post": {
        "canary_runtest_finish": {
          "purpose": "Runs after the test execution phase and before the job is finally saved.",
          "arguments": [
            "case"
          ],
          "default_behavior": "The built-in wrapper calls case.finish() and saves the job.",
          "agent_use": "Inspect outputs, perform post-processing, add measurements, and attach metadata."
        },
        "canary_runtests_report": {
          "purpose": "Runs after a test session to generate reports or summaries.",
          "arguments": [
            "runner"
          ],
          "agent_use": "Create machine-readable reports or summarize structured measurements."
        },
        "canary_sessionfinish": {
          "purpose": "Runs after a non-reused session finishes.",
          "arguments": [
            "session"
          ],
          "agent_use": "Add final session metadata, archive outputs, notify external systems, or trigger follow-on automation."
        }
      },
      "generation": {
        "canary_collectstart": "Register job generator classes with the collector.",
        "canary_generate_modifyitems": "Inspect or mutate generated JobSpecs before they are stored or run.",
        "canary_generate_report": "Report generation results."
      },
      "execution": {
        "canary_runtests": "Override or provide a test-session execution backend.",
        "canary_runtest_launcher": "Return a Launcher for an individual Job.",
        "canary_runteststart": "Setup phase for one Job.",
        "canary_runtest": "Execution phase for one Job.",
        "canary_runtest_finish": "Finish/post-processing phase for one Job."
      },
      "resources": {
        "canary_resource_pool_fill": "Create the initial resource-pool specification.",
        "canary_resource_pool_update": "Mutate or augment a ResourcePool before use.",
        "canary_resource_pool_accommodates": "Determine whether a Job can be accommodated.",
        "canary_resource_pool_types": "Return available resource types."
      }
    },
    "plugins": {
      "summary": "Canary functionality is extended with pluggy plugins registered through the canary entry point group.",
      "entry_point_group": "canary",
      "generators": {
        "purpose": "Add support for new job-definition formats.",
        "interfaces": [
          "AbstractSpecGenerator",
          "canary_collectstart",
          "canary_testcase_generator"
        ],
        "outputs": [
          "JobSpecIR",
          "JobSpec"
        ]
      },
      "commands": {
        "purpose": "Add CLI subcommands.",
        "interfaces": [
          "CanarySubcommand",
          "canary_addcommand"
        ]
      },
      "configuration": {
        "purpose": "Add configuration sections and command-line options.",
        "interfaces": [
          "canary_addconfig",
          "canary_addoption",
          "canary_configure"
        ]
      },
      "launchers": {
        "purpose": "Control how a job command is executed.",
        "interfaces": [
          "Launcher",
          "canary_runtest_launcher"
        ]
      },
      "resources": {
        "purpose": "Provide or modify resource-pool behavior.",
        "interfaces": [
          "canary_resource_pool_fill",
          "canary_resource_pool_update",
          "canary_resource_pool_accommodates"
        ]
      },
      "reporters": {
        "purpose": "Generate structured reports from jobs and sessions.",
        "interfaces": [
          "CanaryReporter",
          "canary_reporter",
          "canary_runtests_report"
        ]
      },
      "query_data": {
        "purpose": "Expose extension-specific capabilities and skills through canary query.",
        "interfaces": [
          "canary_capabilities",
          "canary_skills"
        ]
      }
    },
    "results": {
      "summary": "Canary records status, lifecycle timing, measurements, logs, artifacts, views, and reports.",
      "common_outcomes": [
        "SUCCESS",
        "FAILED",
        "DIFFED",
        "TIMEOUT",
        "SKIPPED",
        "BLOCKED",
        "ERROR",
        "BROKEN",
        "XFAIL",
        "XDIFF",
        "CANCELLED",
        "INTERRUPTED",
        "NONE"
      ],
      "logs": {
        "stdout_default": "canary-out.txt",
        "stderr_default": "stderr is combined with stdout unless configured otherwise",
        "command": "canary log JOBID"
      },
      "views": {
        "default_name": "TestResults",
        "modes": [
          "symlink",
          "hardlink",
          "copy",
          "none"
        ],
        "purpose": "Expose latest results in a user-facing tree."
      },
      "reports": {
        "json": "Machine-readable JSON report.",
        "junit": "JUnit XML report for CI systems.",
        "html": "Static HTML report with job pages and file browser.",
        "markdown": "Markdown report for lightweight summaries."
      }
    },
    "commands": {
      "summary": "Use generated command reference pages for exact syntax. This object provides agent-oriented command selection guidance.",
      "run": {
        "purpose": "Collect/select and execute jobs.",
        "typical": [
          "canary run PATH",
          "canary run TAG",
          "canary run JOBID",
          "canary run PATH -- ARGUMENTS_FOR_TEST_SCRIPT"
        ]
      },
      "collect": {
        "purpose": "Find generators and cache generated specs in a workspace.",
        "typical": [
          "canary collect PATH"
        ]
      },
      "select": {
        "purpose": "Create, rename, delete, or derive tagged selections.",
        "typical": [
          "canary select TAG -k EXPRESSION"
        ]
      },
      "find": {
        "purpose": "Discover and display generated specs without necessarily storing/running them.",
        "typical": [
          "canary find PATH",
          "canary find -g PATH"
        ]
      },
      "log": {
        "purpose": "Display session or job log files.",
        "typical": [
          "canary log JOBID"
        ]
      },
      "location": {
        "purpose": "Print input, log, working, or source locations for a job/spec.",
        "typical": [
          "canary location JOBID",
          "canary location -l JOBID"
        ]
      },
      "query": {
        "purpose": "Query job locks, session locks, capabilities, or skills as JSON.",
        "typical": [
          "canary query -j JOBID measurements.data",
          "canary query -s latest measurements",
          "canary query -c overview",
          "canary query -k canary-orientation"
        ]
      },
      "exec": {
        "purpose": "Execute a single job directly.",
        "typical": [
          "canary exec JOBID"
        ]
      },
      "report": {
        "purpose": "Create reports from stored Canary results.",
        "typical": [
          "canary report json",
          "canary report junit",
          "canary report html",
          "canary report markdown"
        ]
      },
      "config": {
        "purpose": "Inspect and update configuration.",
        "typical": [
          "canary config show",
          "canary config show resource_pool"
        ]
      }
    },
    "agent_workflows": {
      "summary": "Agents can use Canary as a structured execution and observation layer for code development, testing, simulation, and post-processing.",
      "principles": [
        "Use Canary commands to execute work instead of manually orchestrating subprocesses when dependencies, resources, persistence, or reports matter.",
        "Prefer structured measurements over log scraping.",
        "Use canary query to retrieve state and measurements.",
        "Use hooks or generator metadata to attach domain-specific data.",
        "Use selections and rerun strategies to minimize repeated work."
      ],
      "basic_loop": [
        "Write or modify job definitions.",
        "Run canary run PATH or canary run TAG.",
        "Inspect status with canary status.",
        "Query structured data with canary query.",
        "Use measurements and statuses to decide next action."
      ],
      "recommended_queries": [
        "canary query -c overview",
        "canary query -c hooks.post",
        "canary query -j JOBID measurements.data",
        "canary query -s latest measurements",
        "canary query -s latest job_ids"
      ],
      "common_mistakes": [
        "Scraping canary-out.txt for data that could be emitted as a structured measurement.",
        "Assuming one job-definition format is the only way to define jobs.",
        "Ignoring dependency outcomes when interpreting skipped or blocked jobs.",
        "Assuming SQLite contains every artifact or full serialized job state.",
        "Changing resource requirements without checking the resource pool."
      ]
    },
    "apis": {
      "jobs": [
        "canary.Job",
        "_canary.job.Job",
        "_canary.jobspec.JobSpec",
        "_canary.ir.JobSpecIR"
      ],
      "generators": [
        "_canary.generator.AbstractSpecGenerator",
        "_canary.generate.Generator"
      ],
      "workspace": [
        "_canary.workspace.Workspace",
        "_canary.workspace.Session",
        "_canary.database.WorkspaceDatabase"
      ],
      "resources": [
        "_canary.resource_pool.rpool.ResourcePool",
        "_canary.resource_pool.rpool.Node",
        "_canary.resource_pool.rpool.NodeRequest",
        "_canary.resource_pool.manager.ResourceManager"
      ],
      "hooks": [
        "_canary.hookspec"
      ],
      "measurements": [
        "_canary.job.Measurements",
        "_canary.job.Job.add_measurement",
        "_canary.workspace.Session.add_measurement"
      ]
    },
    "limitations": {
      "query": [
        "The query command is intentionally lightweight and not a jq-compatible query engine.",
        "It reads JSON-like lock or static dataset objects and applies simple path selection."
      ],
      "capabilities_database": [
        "The capabilities database is runtime aggregated from core package data and extension plugin contributions.",
        "Generated command/directive reference remains the authority for exact command-line and directive syntax.",
        "Python API docs remain the authority for full method signatures."
      ]
    }
  }
}
